"""Phase 3 lifecycle module — power/snapshot/backup/resize/clone/migrate.

Plan 03-01 ships only ``errors.py`` (the curated PVE-error map). Plans 02/03/04
add the per-operation routes + services.
"""
