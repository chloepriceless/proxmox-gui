{
  "name": "proxmox-gui-frontend-runtime",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Runtime dependencies for the adapter-node server in this directory. Committed alongside the build output so the LXC has no install-time package fetches.",
  "dependencies": {
    "@sveltejs/kit": "2.59.1",
    "@standard-schema/spec": "1.1.0",
    "clsx": "2.1.1",
    "dompurify": "3.4.3",
    "marked": "18.0.3",
    "tailwind-merge": "2.6.1"
  }
}
