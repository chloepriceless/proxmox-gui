---
phase: 03-job-queue-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/clusters/connector.py
  - backend/app/models/job.py
  - backend/alembic/versions/0004_phase3.py
  - backend/app/jobs/__init__.py
  - backend/app/jobs/worker.py
  - backend/app/jobs/functions.py
  - backend/app/jobs/enqueue.py
  - backend/app/jobs/poller.py
  - backend/app/jobs/reaper.py
  - backend/app/jobs/events.py
  - backend/app/jobs/service.py
  - backend/app/jobs/schemas.py
  - backend/app/lifecycle/__init__.py
  - backend/app/lifecycle/errors.py
  - backend/app/main.py
  - deploy/systemd/proxmox-gui-worker.service
  - deploy/lxc/bootstrap.sh
  - backend/tests/test_jobs_infrastructure.py
  - backend/tests/test_lifecycle_errors.py
autonomous: true
requirements: [API-04, LIFE-12, LIFE-13, LIFE-14, UI-06]
must_haves:
  truths:
    - "The arq worker process starts via `arq app.jobs.worker.WorkerSettings` and connects to a localhost Redis"
    - "A jobs row can be enqueued, persisted, and the worker picks it up via its DB id"
    - "On worker boot the orphan reaper scans non-terminal jobs and reconciles each against Proxmox"
    - "A UPID is persisted to the jobs row before the polling loop begins"
    - "A known PVE error substring maps to a curated friendly message; an unknown one falls back to the raw message verbatim"
    - "Redis is installed in the LXC bound to 127.0.0.1 and the worker systemd unit runs arq"
  artifacts:
    - path: "backend/app/jobs/worker.py"
      provides: "arq WorkerSettings + on_startup/on_shutdown hooks + cron registration"
      contains: "class WorkerSettings"
    - path: "backend/app/jobs/enqueue.py"
      provides: "enqueue_job helper — insert jobs row, commit, arq enqueue, return job"
      contains: "idempotency_key"
    - path: "backend/app/jobs/poller.py"
      provides: "UPID polling loop, first-response-authoritative, adaptive cadence"
      contains: "exitstatus"
    - path: "backend/app/jobs/reaper.py"
      provides: "orphan reaper — boot-time non-terminal job reconciliation"
      contains: "needs_review"
    - path: "backend/app/jobs/events.py"
      provides: "Redis pub/sub publish (worker) + subscribe pump (API)"
      contains: "jobs:events"
    - path: "backend/app/lifecycle/errors.py"
      provides: "curated PVE-error → friendly-message map with raw fallback"
      contains: "VM is locked"
    - path: "backend/alembic/versions/0004_phase3.py"
      provides: "0004_phase3 migration — batch_id + friendly_error columns + backup_schedules table"
      contains: "0004_phase3"
  key_links:
    - from: "backend/app/jobs/worker.py"
      to: "backend/app/jobs/reaper.py"
      via: "on_startup hook calls reap_orphans"
      pattern: "reap_orphans"
    - from: "backend/app/jobs/poller.py"
      to: "backend/app/lifecycle/errors.py"
      via: "map_pve_error on failed task"
      pattern: "map_pve_error"
    - from: "deploy/systemd/proxmox-gui-worker.service"
      to: "backend/app/jobs/worker.py"
      via: "ExecStart arq app.jobs.worker.WorkerSettings"
      pattern: "arq app.jobs.worker.WorkerSettings"
---

<objective>
Build the Phase 3 job-queue infrastructure: the arq worker process, the durable UPID-polling loop, the orphan reaper, the Redis pub/sub event channel, the 202-Accepted enqueue helper, the curated PVE-error map, the connector lifecycle/polling method extensions, the `0004_phase3` migration, and the deploy changes (Redis install + worker unit). This is the foundation every other Phase 3 plan builds on — no lifecycle operation can ship until this pipeline exists.

Purpose: Establish the enqueue → worker → UPID-poll → Redis pub/sub pipeline so subsequent plans only add thin per-operation job kinds and routes.
Output: A runnable arq worker, the enqueue contract, the poller/reaper/events modules, the error map, the connector method set, the migration, and the deploy wiring.
</objective>

<execution_context>
@/mnt/claude-config/get-shit-done/workflows/execute-plan.md
@/mnt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-queue-lifecycle/03-CONTEXT.md
@.planning/phases/03-job-queue-lifecycle/03-RESEARCH.md
@.planning/phases/03-job-queue-lifecycle/03-PATTERNS.md

<interfaces>
<!-- Shipped contracts the executor builds against. Do NOT re-explore the codebase. -->

From backend/app/models/job.py (jobs table — schema already shipped Plan 01-02):
```python
class Job(Base):
    __tablename__ = "jobs"
    id: Mapped[int]                        # primary key; also the arq job id
    idempotency_key: Mapped[str | None]    # String(128), nullable, unique
    state: Mapped[str]                     # String(32), default "pending"
    cluster_id: Mapped[int | None]         # FK clusters.id
    team_id: Mapped[int | None]            # FK teams.id
    actor_user_id: Mapped[int | None]      # FK users.id
    kind: Mapped[str]                      # String(64) — e.g. "vm.power"
    payload: Mapped[str]                   # Text, JSON
    upid: Mapped[str | None]               # String(255) — persisted BEFORE polling
    upid_node: Mapped[str | None]          # String(64)
    started_at / finished_at: Mapped[datetime | None]
    error: Mapped[str | None]              # Text
    created_at: Mapped[datetime]           # server_default CURRENT_TIMESTAMP
# State machine: pending → claimed → running → succeeded/failed/orphaned/needs_review
```

From backend/app/clusters/registry.py (per-team connector resolution):
```python
async def get_for_team(self, *, cluster_id: int, team_id: int, db: AsyncSession | None = None) -> PVEConnector
# Raises LookupError when no team_cluster_tokens row exists.
```

From backend/app/clusters/connector.py (existing connector — EXTEND, do not replace):
```python
# Existing helper used by every read/write method — routes through pybreaker + asyncio.to_thread:
async def _call_with_breaker(self, fn, **kwargs) -> Any
# Existing method shape to copy (qemu/lxc branch):
async def get_vm_status(self, *, node: str, vmid: int, is_lxc: bool) -> dict:
    fn = (self._client.nodes(node).lxc(vmid).status.current.get if is_lxc
          else self._client.nodes(node).qemu(vmid).status.current.get)
    return await self._call_with_breaker(fn)
# Exceptions raised by _call_with_breaker: PVEUnreachable, PVEAuthError, PVEAPIError (from app.clusters.errors)
```

From backend/app/clusters/errors.py: `PVEAPIError` carries a `.status_code` attribute.

From backend/app/main.py (lifespan + router registration — extend):
```python
# create_app() builds app.state.registry in the lifespan; routers are included
# locally inside create_app via app.include_router(router, prefix="/api/v1", tags=[...])
```

From deploy/systemd/proxmox-gui-worker.service (Phase 1 placeholder — sets these env vars already):
```
Environment=PROXMOX_GUI_MASTER_KEY_PATH=/etc/proxmox-gui/master.key
Environment=PROXMOX_GUI_DATABASE_URL=sqlite+aiosqlite:////var/lib/proxmox-gui/app.db
ExecStart=/bin/sh -c 'echo "worker placeholder — phase 3 wires arq"; sleep infinity'
After=network-online.target proxmox-gui-api.service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependencies, 0004_phase3 migration, and connector lifecycle/polling methods</name>
  <files>backend/pyproject.toml, backend/app/models/job.py, backend/alembic/versions/0004_phase3.py, backend/app/clusters/connector.py</files>
  <read_first>
    - backend/pyproject.toml (current dependency list)
    - backend/app/clusters/connector.py (full file — the class being extended; copy the `_call_with_breaker` + qemu/lxc-branch method shape)
    - backend/app/models/job.py (the jobs table being extended)
    - backend/alembic/versions/0003_phase2.py (the exact migration analog — copy structure, revision header, batch_alter_table pattern)
    - backend/alembic/env.py (confirm render_as_batch=True is set)
  </read_first>
  <behavior>
    - Test: importing `app.clusters.connector` exposes the new methods `vm_power`, `vm_delete`, `task_status`, `task_log`, `snapshot_list`, `snapshot_create`, `snapshot_rollback`, `snapshot_delete`, `vzdump`, `restore`, `resize_disk`, `clone`, `to_template`, `migrate`, `cluster_status`, `cluster_nextid`, `node_storages`, `unlock` on `PVEConnector`.
    - Test: `vm_power` against a FakeProxmox returns the recorded UPID string and routes through `_call_with_breaker` (not `_call`).
    - Test: `alembic upgrade head` then `alembic downgrade -1` round-trips the `0004_phase3` migration cleanly on a temp SQLite DB.
    - Test: after migration, the `jobs` table has `batch_id` and `friendly_error` columns and a `backup_schedules` table exists.
  </behavior>
  <action>
1. **backend/pyproject.toml** — add to the `dependencies` list, alphabetically near existing entries:
   ```
   "arq==0.26.3",
   "redis[hiredis]==5.3.1",
   ```
   (`proxmoxer`, `pybreaker` are already present.) Then run `cd backend && pip install -e .`.

2. **backend/app/models/job.py** — add two nullable columns to the `Job` class after `error`:
   ```python
   batch_id: Mapped[str | None] = mapped_column(String(64), nullable=True)
   friendly_error: Mapped[str | None] = mapped_column(Text, nullable=True)
   ```
   Add an index to `__table_args__`: `Index("ix_jobs_batch_id", "batch_id")`. Update the module docstring's state-machine note to mention `batch_id` (D-11 grouping) and `friendly_error` (D-13 curated message).

3. **backend/alembic/versions/0004_phase3.py** — copy `0003_phase2.py` structure verbatim. Revision header:
   ```python
   revision: str = "0004_phase3"
   down_revision: str | None = "0003_phase2"
   branch_labels: str | Sequence[str] | None = None
   depends_on: str | Sequence[str] | None = None
   ```
   `upgrade()`:
   - `with op.batch_alter_table("jobs") as batch_op:` add `batch_op.add_column(sa.Column("batch_id", sa.String(64), nullable=True))` and `batch_op.add_column(sa.Column("friendly_error", sa.Text(), nullable=True))`.
   - `op.create_index("ix_jobs_batch_id", "jobs", ["batch_id"])` outside the batch block.
   - Create `backup_schedules` table (for LIFE-06, Plan 04 populates it; create the table now so the schema is stable): columns `id` (Integer PK), `cluster_id` (Integer, ForeignKey `clusters.id`, nullable=False, FK name `fk_backup_schedules_cluster_id`), `team_id` (Integer, ForeignKey `teams.id`, nullable=False, FK name `fk_backup_schedules_team_id`), `vmid` (Integer, nullable=False), `is_lxc` (Boolean, nullable=False, server_default `sa.text("0")`), `node` (String(64), nullable=False), `enabled` (Boolean, nullable=False, server_default `sa.text("1")`), `frequency` (String(16), nullable=False — values `daily`/`weekly`), `keep_last` (Integer, nullable=False, server_default `sa.text("7")`), `last_run_at` (DateTime, nullable=True), `last_run_state` (String(16), nullable=True), `created_at` (DateTime, nullable=False, server_default `sa.text("CURRENT_TIMESTAMP")`). Add a named unique constraint `uq_backup_schedules_cluster_vmid` on `("cluster_id", "vmid")` and a named index `ix_backup_schedules_enabled` on `("enabled",)`.
   `downgrade()`: drop the index `ix_backup_schedules_enabled`, drop table `backup_schedules`, drop index `ix_jobs_batch_id`, then `with op.batch_alter_table("jobs") as batch_op:` drop `friendly_error` and `batch_id` columns.

4. **backend/app/clusters/connector.py** — add the following methods to the existing `PVEConnector` class. Every method routes through `self._call_with_breaker` (NOT `_call`). Use the `fn = (... lxc ... if is_lxc else ... qemu ...)` branch shape. After any mutating call, set `self._resource_cache.snapshot = None`. Exact signatures and proxmoxer call shapes (verified against RESEARCH §Proxmox Call Reference):
   ```python
   async def vm_power(self, *, node, vmid, is_lxc, action) -> str:
       # action ∈ {start, stop, reboot, shutdown}. POST status/{action}. Returns UPID.
       base = self._client.nodes(node).lxc(vmid) if is_lxc else self._client.nodes(node).qemu(vmid)
       fn = getattr(base.status, action).post
       upid = await self._call_with_breaker(fn)
       self._resource_cache.snapshot = None
       return upid

   async def vm_delete(self, *, node, vmid, is_lxc) -> str:
       # DELETE qemu/lxc with purge=1. NEVER pass skiplock. Returns UPID.
       fn = (self._client.nodes(node).lxc(vmid).delete if is_lxc
             else self._client.nodes(node).qemu(vmid).delete)
       upid = await self._call_with_breaker(fn, purge=1)
       self._resource_cache.snapshot = None
       return upid

   async def task_status(self, *, node, upid) -> dict:
       # GET /nodes/{node}/tasks/{upid}/status -> {status, exitstatus?, ...}
       fn = self._client.nodes(node).tasks(upid).status.get
       return await self._call_with_breaker(fn)

   async def task_log(self, *, node, upid, limit=200) -> str:
       from proxmoxer.tools import Tasks
       fn = self._client.nodes(node).tasks(upid).log.get
       raw = await self._call_with_breaker(fn, limit=limit)
       return Tasks.decode_log(raw)
   ```
   Add the remaining methods following the same pattern (full signatures in RESEARCH §"PVEConnector extension" and §Proxmox Call Reference): `snapshot_list(node,vmid,is_lxc)` → `base.snapshot.get` (sync list); `snapshot_create(node,vmid,is_lxc,snapname,description,vmstate)` → `base.snapshot.post(...)` UPID; `snapshot_rollback(node,vmid,is_lxc,name)` → `base.snapshot(name).rollback.post` UPID; `snapshot_delete(node,vmid,is_lxc,name)` → `base.snapshot(name).delete` UPID; `vzdump(node,vmid,storage,mode='snapshot',compress='zstd')` → `self._client.nodes(node).vzdump.post(vmid=,storage=,mode=,compress=)` UPID; `restore(node,vmid,archive,is_lxc,force=False,storage=None)` → qemu: `nodes(node).qemu.post(vmid=,archive=,force=...)`, lxc: `nodes(node).lxc.post(vmid=,ostemplate=archive,restore=1,force=...)` UPID; `resize_disk(node,vmid,is_lxc,disk,size)` → `base.resize.put(disk=,size=)` sync; `clone(node,vmid,newid,name,full,target,storage,is_lxc)` → `base.clone.post(newid=,name=,full=,target=,storage=)` UPID; `to_template(node,vmid)` → `self._client.nodes(node).qemu(vmid).template.post` UPID (qemu-only — A7); `migrate(node,vmid,is_lxc,target,online,bwlimit)` → `base.migrate.post(target=,online=,bwlimit=)` UPID; `cluster_status()` → `self._client.cluster.status.get` sync; `cluster_nextid()` → `self._client.cluster.nextid.get` sync; `node_storages(node,content='backup')` → `self._client.nodes(node).storage.get(content=content)` sync; `unlock(node,vmid,is_lxc)` → best-effort `base.config.put(lock='')` (RESEARCH Open Question Q1 — wrap so a PVE rejection surfaces as a normal PVEAPIError, do NOT send skiplock).
   For CPU/RAM resize (no UPID — sync config write) the connector's existing `set_vm_config` is reused; no new method needed.

5. Drop a `Sequence` import into `0004_phase3.py` if the type annotations need it (`from typing import Sequence`). Run `cd backend && ruff check .` and fix any lint.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.clusters.connector import PVEConnector; assert all(hasattr(PVEConnector,m) for m in ['vm_power','vm_delete','task_status','task_log','snapshot_list','snapshot_create','snapshot_rollback','snapshot_delete','vzdump','restore','resize_disk','clone','to_template','migrate','cluster_status','cluster_nextid','node_storages','unlock'])" && python -m pytest tests/test_jobs_infrastructure.py -x -q && ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'arq==0.26.3' backend/pyproject.toml` returns 1 and `grep -c 'redis\[hiredis\]==5.3.1' backend/pyproject.toml` returns 1.
    - `grep -E 'batch_id|friendly_error' backend/app/models/job.py` shows both new columns.
    - `grep 'revision: str = "0004_phase3"' backend/alembic/versions/0004_phase3.py` matches.
    - `grep 'down_revision: str | None = "0003_phase2"' backend/alembic/versions/0004_phase3.py` matches.
    - `grep -c 'backup_schedules' backend/alembic/versions/0004_phase3.py` is ≥ 2 (create + drop).
    - `grep -c '_call_with_breaker' backend/app/clusters/connector.py` increased by at least 14 over the pre-task count (one per new method that issues a PVE call).
    - `grep -c 'skiplock' backend/app/clusters/connector.py` returns 0.
    - `cd backend && alembic upgrade head && alembic downgrade -1 && alembic upgrade head` exits 0.
  </acceptance_criteria>
  <done>The dependencies are installed, the `0004_phase3` migration round-trips cleanly, and `PVEConnector` exposes all 18 lifecycle/polling methods routed through the circuit breaker with no `skiplock` anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curated PVE-error map and the jobs package (worker, enqueue, poller, reaper, events, service, schemas)</name>
  <files>backend/app/lifecycle/__init__.py, backend/app/lifecycle/errors.py, backend/app/jobs/__init__.py, backend/app/jobs/schemas.py, backend/app/jobs/service.py, backend/app/jobs/enqueue.py, backend/app/jobs/events.py, backend/app/jobs/poller.py, backend/app/jobs/reaper.py, backend/app/jobs/worker.py, backend/app/jobs/functions.py, backend/app/main.py</files>
  <read_first>
    - backend/app/clusters/errors.py (PVEAPIError / PVEUnreachable / PVEAuthError — the exception surface)
    - backend/app/audit/writer.py (the flush-not-commit transaction discipline to copy in enqueue.py)
    - backend/app/inventory/service.py (commit-before-raise pattern, connector acquisition via registry.get_for_team)
    - backend/app/clusters/health.py (background async loop shape — the poller analog)
    - backend/app/teams/bootstrap.py (idempotent sweep shape — the reaper analog)
    - backend/app/inventory/schemas.py (pydantic schema conventions for schemas.py)
    - .planning/phases/03-job-queue-lifecycle/03-RESEARCH.md §"Architecture Patterns" Patterns 1-5 (the literal sketches for worker/enqueue/poller/reaper/events)
  </read_first>
  <behavior>
    - Test: `map_pve_error("can't lock file ...", "")` returns the friendly string "VM is locked — unlock it from the detail page, then retry."
    - Test: `map_pve_error("some totally novel pve error", "")` returns the raw string verbatim (never "operation failed").
    - Test: `map_pve_error` matching is case-insensitive substring (e.g. "VM IS LOCKED" still maps).
    - Test: `enqueue_job` inserts a jobs row in state `pending`, commits it, and a second call with identical (kind, actor, payload) returns the SAME job id (idempotency-key collision returns the in-flight job, not a duplicate).
    - Test: `WorkerSettings` exposes `functions`, `cron_jobs`, `on_startup`, `on_shutdown`, `redis_settings` and every `func()` entry has `max_tries=1`.
    - Test: the reaper, given a job row with `upid` set whose `task_status` returns `{"status":"stopped","exitstatus":"OK"}`, resolves the job to `succeeded`; given a `claimed`/`running` row with NO `upid`, marks it `needs_review`.
  </behavior>
  <action>
1. **backend/app/lifecycle/__init__.py** — empty package marker (copy `backend/app/inventory/__init__.py`).

2. **backend/app/lifecycle/errors.py** — the curated PVE-error map (D-13, UI-06). Define `map_pve_error(exitstatus: str | None, log_tail: str = "") -> str`. It concatenates `exitstatus` + `log_tail`, lowercases for matching, and walks an ordered list of `(substrings_tuple, friendly_message)` rules. On the FIRST rule whose any-substring matches, return that rule's friendly message. If nothing matches, return the raw `exitstatus` (or `log_tail` if `exitstatus` is empty) verbatim — NEVER the string "operation failed". Reproduce the friendly copy VERBATIM from UI-SPEC §"Error Presentation Contract" curated table (these strings are locked):
   - `can't lock file`, `vm is locked`, `ct is locked`, `unable to acquire lock` → "VM is locked — unlock it from the detail page, then retry."
   - `not enough memory`, `would exceed`, `insufficient`, `no space left on device` (node context) → "The target node doesn't have enough free CPU or memory. Pick another node."
   - `storage`+`does not exist`, `is not online`, `unable to activate storage`, `storage is disabled` → "The storage for this operation isn't available right now. Check the cluster, then retry."
   - `cicustom`, `snippet`, `volume`+`does not exist` → "This VM references a file that only exists on its current node. It can't be migrated until that's resolved."
   - `cluster not ready`, `no quorum`, `quorum` → "The Proxmox cluster has lost quorum — writes are paused until it recovers."
   - `storage full`, `not enough free space` (backup context) → "The backup storage is out of space. Free up space or reduce retention."
   - `unable to shrink`, `shrink`, `can't shrink disk` → "Disks can only grow. Shrinking is not supported by Proxmox."
   - `timeout`, `got timeout`, `connection timed out` → "Couldn't reach the Proxmox node in time. It may be busy — try again shortly."
   - `permission check failed`, `no permission`, `only root` → "Your team's token can't perform this action on this resource. Contact an administrator."
   Add a module docstring noting D-13 (curated + raw fallback, never swallowed) and D-15 (no redaction — full raw detail shown to all users; this is a conscious accepted deviation, do not add redaction).

3. **backend/app/jobs/__init__.py** — empty package marker.

4. **backend/app/jobs/schemas.py** — pydantic models `JobResponse` (id, kind, state, cluster_id, team_id, upid, upid_node, error, friendly_error, batch_id, created_at, started_at, finished_at) and `JobListResponse` (jobs: list[JobResponse], plus running/failed counts). Copy the `ConfigDict`/field conventions from `inventory/schemas.py`. Add `serialize_job(job: Job) -> dict` returning a JSON-safe dict (used by `events.py` for pub/sub payloads).

5. **backend/app/jobs/service.py** — job-row CRUD: `get_job(db, job_id)`, `list_jobs(db, team_ids, limit=50, state=None)` (team-scoped per D-01, newest-first), `list_recent_jobs(db, team_ids, limit=50)` (for WS backfill), `update_job(db, job_id, **fields)`, `finish_job(db, job_id, state, error=None, friendly=None, log=None)` (sets `finished_at`), `find_job_by_idempotency_key(db, key)`, `select_jobs(db, states)` (reaper helper). All functions flush; callers own the commit (audit/writer.py discipline).

6. **backend/app/jobs/enqueue.py** — implement `enqueue_job` exactly per RESEARCH §Pattern 2: compute `idempotency_key = sha256(json.dumps({'kind','actor','payload'}, sort_keys=True))[:128]`; insert the `Job` row (`state='pending'`, `batch_id` optional); `db.flush()` to surface the UNIQUE collision; on `IntegrityError` → `db.rollback()` + return `find_job_by_idempotency_key(...)` (the in-flight job); else `db.commit()` (commit BEFORE enqueue — non-negotiable ordering); then `await arq_pool.enqueue_job(kind, job.id, _job_id=f'job-{job.id}')`; return the job. Signature: `async def enqueue_job(db, arq_pool, *, kind, cluster_id, team_id, actor_user_id, payload, batch_id=None) -> Job`.

7. **backend/app/jobs/events.py** — Redis pub/sub per RESEARCH §Pattern 5. `publish_event(redis, event_type, job)` publishes `json.dumps({'type':event_type,'job':serialize_job(job)})` to channel `jobs:events`. `ConnectionManager` class: `add(websocket, team_ids)`, `remove(websocket)`, `broadcast(event)` — broadcast filters each event by the socket's `team_ids` (the job's `team_id` must intersect; re-checked on every push per Pitfall 9). `jobs_event_pump(app)` — the API-side subscriber coroutine that listens on `jobs:events` and calls `CONNECTION_MANAGER.broadcast`. Export a module-level `CONNECTION_MANAGER` singleton.

8. **backend/app/jobs/poller.py** — `dispatch_and_poll(ctx, job, connector, dispatch_fn)` exactly per RESEARCH §Pattern 3: call `dispatch_fn()` → UPID; `Tasks.decode_upid(upid)['node']` for the node; persist `upid`+`upid_node`+`state='running'`+`started_at` to the jobs row BEFORE polling (Pitfall 12); publish `job.running`; poll loop with adaptive cadence `delay=0.5; delay=min(delay*1.6, 30.0)`; treat the FIRST `status=='stopped'` response as authoritative (Pitfall 2); on terminal read `task_log`, on `exitstatus=='OK'` → `finish_job(succeeded)`, on `exitstatus` starting with `WARNINGS:` → `finish_job(succeeded)` with the warning surfaced (RESEARCH A3 / Open Question 3), otherwise → `finish_job(failed)` with `map_pve_error(exitstatus, log_tail)` stored in `friendly_error`; publish `job.completed`. Between polls publish `job.progress`.

9. **backend/app/jobs/reaper.py** — `reap_orphans(ctx)` exactly per RESEARCH §Pattern 4 covering all 5 edge cases: `upid` set + running → re-enqueue a `job.reattach` poll job, mark `orphaned` transiently; `upid` set + stopped → resolve directly; `upid` set + PVE 404 → `needs_review`; no `upid` + `pending` → re-enqueue normally; no `upid` + `claimed`/`running` → `needs_review` ("Worker died before Proxmox returned a UPID; outcome unknown"). Publish `reaper.reattached` with the re-attached job ids.

10. **backend/app/jobs/worker.py** — `WorkerSettings` per RESEARCH §Pattern 1. For THIS plan, register a placeholder `functions` list with a single internal `func(noop_job, name='internal.noop', max_tries=1)` plus the `cron_jobs` list empty for now — Plans 02/03/04 add the real `vm.*` job functions to this list. Implement `on_startup(ctx)`: open the app DB engine, install the cipher (`from app.core.cipher import install_cipher` — worker process needs it to decrypt cluster tokens; read how the API process does this in `main.py`), build a `PVEConnectorRegistry`, store engine/registry/sessionmaker on `ctx`, then `await reap_orphans(ctx)` (LIFE-14 — runs on every boot, no exceptions). `on_shutdown(ctx)`: `await ctx['engine'].dispose()`. `WorkerSettings`: `redis_settings = RedisSettings(host='127.0.0.1', port=6379, database=0)`, `max_jobs=6`, `job_timeout=14400`, `keep_result=3600`, `health_check_interval=30`. Add a `# NOTE: Plans 02/03/04 register vm.* job functions in `functions`` comment so downstream plans know where to extend.

11. **backend/app/jobs/functions.py** — create the file with a `noop_job(ctx) -> None` stub and a module docstring explaining Plans 02/03/04 add the real `run_*` job functions here. (Keeps the import in `worker.py` valid; downstream plans extend.)

12. **backend/app/main.py** — in the lifespan, create the API-side arq Redis pool via `arq.create_pool(RedisSettings(host='127.0.0.1', port=6379, database=0))`, store on `app.state.arq_pool`; start `jobs_event_pump(app)` as a background task; on shutdown close the pool and cancel the pump task. Do NOT register lifecycle/jobs routers yet — Plan 02 does that.

All service/job code follows commit-before-raise (audit/writer.py discipline). The arq worker is a SEPARATE process — `worker.py` is not imported by `main.py`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_lifecycle_errors.py tests/test_jobs_infrastructure.py -x -q && python -c "from app.jobs.worker import WorkerSettings; from arq import func; assert all(getattr(f,'max_tries',None)==1 for f in WorkerSettings.functions)" && ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"VM is locked — unlock it from the detail page, then retry."' backend/app/lifecycle/errors.py` returns 1.
    - `grep -c 'operation failed' backend/app/lifecycle/errors.py` returns 0.
    - `grep -c 'jobs:events' backend/app/jobs/events.py` is ≥ 2 (publish + subscribe).
    - `grep 'await db.commit()' backend/app/jobs/enqueue.py` matches AND appears before the line containing `enqueue_job(` in `enqueue.py` (commit-before-enqueue ordering).
    - `grep -c 'decode_upid' backend/app/jobs/poller.py` is ≥ 1 and `grep -c "split(':')" backend/app/jobs/poller.py` returns 0.
    - `grep -c 'needs_review' backend/app/jobs/reaper.py` is ≥ 2.
    - `grep 'reap_orphans' backend/app/jobs/worker.py` matches (reaper called from on_startup).
    - `grep -c 'RedisSettings' backend/app/jobs/worker.py` is ≥ 1 and `grep "host='127.0.0.1'" backend/app/jobs/worker.py` matches (loopback bind).
    - `cd backend && python -m pytest tests/test_lifecycle_errors.py -x -q` passes.
  </acceptance_criteria>
  <done>The curated error map (with raw fallback), the enqueue helper (commit-before-enqueue, idempotency-key dedup), the UPID poller (persist-before-poll, first-response-authoritative), the orphan reaper (5 edge cases), the Redis pub/sub events module, and the arq `WorkerSettings` all exist and pass their tests.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy wiring — Redis install, worker systemd unit, bootstrap.sh upgrade-trap fix</name>
  <files>deploy/systemd/proxmox-gui-worker.service, deploy/lxc/bootstrap.sh</files>
  <read_first>
    - deploy/lxc/bootstrap.sh (full file — the apt block ~line 75, the systemd block ~lines 216-253, and the idempotent-exit short-circuit branch near the top)
    - deploy/systemd/proxmox-gui-worker.service (the Phase 1 placeholder unit to rewrite)
    - deploy/systemd/proxmox-gui-api.service (the hardening directive block to keep consistent)
    - deploy/README.md (if present — to document the Redis config delta)
  </read_first>
  <action>
1. **deploy/systemd/proxmox-gui-worker.service** — rewrite the `ExecStart` line. Replace:
   ```
   ExecStart=/bin/sh -c 'echo "worker placeholder — phase 3 wires arq"; sleep infinity'
   ```
   with:
   ```
   ExecStart=/opt/proxmox-gui/.venv/bin/arq app.jobs.worker.WorkerSettings
   ```
   (Confirm the venv path against `proxmox-gui-api.service`'s `ExecStart` — use the SAME venv path that unit uses.) Change the `[Unit]` section: `After=network-online.target proxmox-gui-api.service redis-server.service` and add `Requires=redis-server.service`. Keep the entire hardening block (`NoNewPrivileges`, `PrivateTmp`, `ProtectSystem=full`, `ProtectHome`, `ReadWritePaths=/var/lib/proxmox-gui /var/log/proxmox-gui`) verbatim. Keep the two `Environment=` lines. Update the header comment from "Phase 3 wires this" to note arq is now wired.

2. **deploy/lxc/bootstrap.sh — apt block** (~line 75): add `redis-server` to the `apt-get install -y -qq \` package list (a new continuation line, e.g. after `caddy`).

3. **deploy/lxc/bootstrap.sh — Redis hardening**: after the apt install, add a step that ensures Redis binds loopback only. Redis bound `127.0.0.1` is mandatory (RESEARCH §Security Domain — never `0.0.0.0`). The Debian default `/etc/redis/redis.conf` already ships `bind 127.0.0.1 -::1` and `protected-mode yes`; add an idempotent guard that verifies/enforces it: `grep -qE '^bind 127\.0\.0\.1' /etc/redis/redis.conf || echo 'bind 127.0.0.1 -::1' >> /etc/redis/redis.conf`. Then `systemctl enable --now redis-server`.

4. **deploy/lxc/bootstrap.sh — systemd block** (~line 253): the worker line is currently commented out: `# systemctl enable --now proxmox-gui-worker.service  # Phase 3 wires arq`. Uncomment it: `systemctl enable --now proxmox-gui-worker.service`. The worker unit must be re-installed too — the `install -m 0644 .../proxmox-gui-worker.service ...` line already exists; ensure `systemctl daemon-reload` runs before the `enable --now`.

5. **deploy/lxc/bootstrap.sh — upgrade-trap fix** (RESEARCH §Runtime State Inventory + Pitfall 10): the idempotent-exit short-circuit branch near the top of the script currently runs ONLY `alembic upgrade head` then `exit 0` — it skips `pip install`, so a Phase-3 deploy onto an existing install never gets `arq`/`redis` in the venv and the worker crashes on import. Fix: in that idempotent-exit branch, add a `pip install -e .` (or the project's exact venv pip-install invocation — copy it from the non-marker first-install path) BEFORE the `alembic upgrade head` line, and add the `apt-get install -y -qq redis-server` + Redis-bind guard + `systemctl daemon-reload` + `systemctl enable --now redis-server proxmox-gui-worker.service` steps to the idempotent branch as well. The idempotent branch must leave a fully-working Phase-3 install. Keep the branch idempotent (re-running must not corrupt state — DEPLOY-02).

6. **deploy/README.md** (if it exists) — add a short paragraph: a 4th runtime service, `redis-server` (Debian stock package), now runs in the LXC bound to `127.0.0.1:6379`, auth-less on loopback; the worker unit depends on it. If `deploy/README.md` does not exist, skip — do not create it.

Run `shellcheck deploy/lxc/bootstrap.sh` and fix any new warnings introduced.
  </action>
  <verify>
    <automated>shellcheck deploy/lxc/bootstrap.sh && grep -q 'arq app.jobs.worker.WorkerSettings' deploy/systemd/proxmox-gui-worker.service && grep -q 'Requires=redis-server.service' deploy/systemd/proxmox-gui-worker.service && grep -q 'redis-server' deploy/lxc/bootstrap.sh</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck deploy/lxc/bootstrap.sh` exits 0.
    - `grep -c 'sleep infinity' deploy/systemd/proxmox-gui-worker.service` returns 0 (placeholder removed).
    - `grep -c 'arq app.jobs.worker.WorkerSettings' deploy/systemd/proxmox-gui-worker.service` returns 1.
    - `grep -c 'Requires=redis-server.service' deploy/systemd/proxmox-gui-worker.service` returns 1.
    - `grep -c 'redis-server' deploy/lxc/bootstrap.sh` is >= 2 (apt install + systemctl enable).
    - `grep -cE '^[[:space:]]*systemctl enable --now proxmox-gui-worker' deploy/lxc/bootstrap.sh` is >= 1 (uncommented, not behind a `#`).
    - `grep -c 'bind 127.0.0.1' deploy/lxc/bootstrap.sh` is >= 1 (Redis loopback guard).
    - The idempotent-exit branch of bootstrap.sh contains a `pip install` invocation (verify by reading the branch — `pip install` appears before its `exit 0`).
  </acceptance_criteria>
  <done>Redis is installed and loopback-bound by bootstrap.sh, the worker systemd unit runs `arq app.jobs.worker.WorkerSettings` with a Redis dependency, the worker is enabled on install, and the bootstrap idempotent-exit branch re-runs `pip install` so a Phase-3 upgrade deploy gets arq/redis in the venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Authenticated HTTP; mutations carry CSRF; this plan adds no routes but sets up the enqueue contract Plan 02 uses |
| API process → Redis | Localhost-only job queue + pub/sub channel |
| worker process → Proxmox | Per-team privsep token; untrusted PVE response data flows back through the poller |
| worker process → Redis | Job claim + event publish |
| Redis ↔ LXC network | Redis must never be reachable outside the LXC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering / Elevation | Embedded Redis | mitigate | bootstrap.sh enforces `bind 127.0.0.1 -::1` + `protected-mode yes` in `/etc/redis/redis.conf`; Redis is LXC-loopback-only, never `0.0.0.0`. Acceptance criterion greps the bind guard. |
| T-03-01-02 | Tampering | Job enqueue / idempotency | mitigate | `idempotency_key = sha256(kind+actor+payload)` includes `actor_user_id` — a different actor produces a different key, so a forged key cannot replay another user's job. UNIQUE constraint makes a double-submit collide and return the in-flight job, not a duplicate. |
| T-03-01-03 | Information Disclosure | Redis pub/sub event channel | mitigate | `ConnectionManager.broadcast` filters every event by the socket's `team_ids` (job `team_id` must intersect), re-checked on every push (Pitfall 9). Plan 02 wires the WS endpoint; the filter logic ships here. |
| T-03-01-04 | Elevation of Privilege | `skiplock` parameter | mitigate | `skiplock` is never sent, never a connector argument, never a schema field — `vm_delete` passes only `purge=1`. Acceptance criterion greps `connector.py` for 0 occurrences of `skiplock`. |
| T-03-01-05 | Information Disclosure | Worker decrypts cluster tokens | accept | The worker process holds the same `EncryptedSecret` cipher as the API process; both are equally trusted. The worker unit keeps the Phase-1 hardening (`PrivateTmp`, `ProtectHome`, `NoNewPrivileges`). Same posture as the existing API process — accepted. |
| T-03-01-06 | Denial of Service | Unbounded job execution | mitigate | `WorkerSettings.max_jobs=6` caps concurrency; `job_timeout=14400` and per-`func` timeouts bound runtime; `max_tries=1` prevents arq from silently re-running a failed non-idempotent job. |
| T-03-01-07 | Information Disclosure | Raw PVE error text in `friendly_error` | accept | D-15 — all users see the full raw PVE detail; no redaction. Conscious documented deviation from Pitfall 24, accepted for the small-team home-lab audience. The error map only adds friendly text; it never hides the raw fallback. Not a finding — a decision. |
</threat_model>

<verification>
- `cd backend && python -m pytest -x -q` — full suite green (existing 249 + new infrastructure tests).
- `cd backend && alembic upgrade head && alembic downgrade -1 && alembic upgrade head` — migration round-trips.
- `cd backend && ruff check .` — clean.
- `shellcheck deploy/lxc/bootstrap.sh` — clean.
- `cd backend && python -c "from app.jobs.worker import WorkerSettings"` — worker module imports without error.
</verification>

<success_criteria>
- The arq worker module imports and exposes `WorkerSettings` with `max_tries=1` on every registered function.
- The `0004_phase3` migration applies and reverses cleanly; `jobs` has `batch_id` + `friendly_error`; `backup_schedules` table exists.
- `PVEConnector` exposes all 18 lifecycle/polling methods, every one routed through `_call_with_breaker`, zero `skiplock` references.
- `map_pve_error` returns the locked friendly strings for known patterns and the raw message verbatim for unknown ones — never "operation failed".
- The enqueue helper commits the jobs row before enqueueing and dedups on the idempotency key.
- The orphan reaper handles all 5 boot-time edge cases.
- bootstrap.sh installs loopback-bound Redis, the worker unit runs arq, and the idempotent-exit branch re-runs `pip install`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-queue-lifecycle/03-01-SUMMARY.md`
</output>
